name: naver-api-live

on:
  push:
    branches: ["poc/naver-telegram-e2e"]
    paths:
      - "scripts/naver_flight_probe.py"
      - ".github/workflows/naver-api-live.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run production Naver SSE POC for CJJ-TPE round trip
        run: python scripts/naver_flight_probe.py
